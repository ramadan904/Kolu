node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts
.env
.env.local
.DS_Store
coverage/
config/mints.json

# Vercel CLI. `vercel pull` writes .vercel/.env.*.local, which holds real
# secret values — this must never be committed.
.vercel
